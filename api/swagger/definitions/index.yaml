Reporting:
  $ref: ./reporting.yaml
GeneralResponse:
  properties:
    message:
      type: string
  required:
  - message
ErrorResponse:
  required:
     - message
  properties:
    message:
      type: string
